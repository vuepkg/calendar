---
"@vuepkg/calendar": minor
---

**IMP-02** `weekdayLabels?: string[]` prop — customize Month view weekday header labels (defaults to `['SUN', 'MON', ..., 'SAT']`, backward compatible)

**IMP-03** `startHour?: number` / `endHour?: number` props — customize the Week/Day timed grid's visible hour range (defaults to `0`~`23`)
